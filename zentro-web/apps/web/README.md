# apps/web

Public marketing/landing site **+** the customer web app (`zentro.com`).

> Generated next with `pnpm create next-app` (App Router, TS, Tailwind).
> Starts with the landing page and its two CTAs: "Find a service" / "Become a partner".
